# Smart Expense Tracker - Backend

This is the backend (Node.js + Express + MongoDB) for the Smart Expense Tracker app.

## Run locally
1. Copy `.env.example` → `.env`
2. Add your MongoDB URI and JWT secret
3. Run:

npm install
npm run dev


## Environment Variables
- MONGO_URI
- JWT_SECRET
- PORT
